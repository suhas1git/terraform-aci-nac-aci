version: '>= 0.14.0'

formatter: markdown table

# Keep Providers column on required_providers constraints (>= 2.0.0), not the
# exact version from .terraform.lock.hcl, so local pre-commit matches CI.
settings:
  lockfile: false

content: |-
  # Terraform ACI External Endpoint Group Module

  Manages ACI External Endpoint Group

  Location in GUI:
  `Tenants` » `XXX` » `Networking` » `L3outs` » `XXX` » `External EPGs`

  ## Examples

  ```hcl
  {{ include "./examples/complete/main.tf" }}
  ```

  {{ .Requirements }}

  {{ .Providers }}

  {{ .Inputs }}

  {{ .Outputs }}

  {{ .Resources }}

output:
  file: README.md
  mode: replace

sort:
  enabled: false
