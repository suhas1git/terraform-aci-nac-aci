version: '>= 0.14.0'

formatter: markdown table

# Providers table: use required_providers constraints, not .terraform.lock.hcl pins,
# so README matches CI and local pre-commit does not flip 2.x.y vs >= 2.0.0.
settings:
  lockfile: false

content: |-
  # Terraform ACI Endpoint Group Module

  Manages ACI Endpoint Group

  Location in GUI:
  `Tenants` » `XXX` » `Application Profiles` » `XXX` » `Application EPGs`

  ## Examples

  ```hcl
  {{ include "./examples/complete/main.tf" }}
  ```

  {{ .Requirements }}

  {{ .Providers }}

  {{ .Inputs }}

  {{ .Outputs }}

  {{ .Resources }}

output:
  file: README.md
  mode: replace

sort:
  enabled: false
